import React from 'react';

export const metadata = {
    title: 'Home'
};

const Homepage = () => {
    return (
        <div>
            <h2 className={``}>Prostore</h2>
        </div>
    );
};

export default Homepage;